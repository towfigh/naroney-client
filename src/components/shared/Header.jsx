import React from 'react';

const Header = ({ title }) => {
	return (
		<div className="header d-flex justify-content-center align-items-center">
			<div className="container">
				<h1 className="header_title">نارُنِی</h1>
			</div>
		</div>
	);
};

export default Header;
